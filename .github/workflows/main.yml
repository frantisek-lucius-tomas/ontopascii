# .github/workflows/publish-to-pypi.yml
name: Publish Python Package

on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'  # Trigger the workflow when a new tag is pushed

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install setuptools wheel twine

    - name: Build the package
      run: |
        python setup.py sdist bdist_wheel

    - name: Publish to PyPI
      env:
        TWINE_USERNAME: __token__  # Use the special token username
        TWINE_PASSWORD: ${{ pypi-AgEIcHlwaS5vcmcCJGY2MzMxODI0LTA1OTgtNDYyNy05YTMzLWFkN2FhNWNiNDE0ZAACKlszLCJmYmMzNjczZi05NWM3LTRlZjAtOWI5NC0wZWI1ZDI4ZDYxZmIiXQAABiC946HziMwY3e4onSwvAfaLObeAjwWUbilLSdpLYsxeSQ }}  # Use a secret for the API token
      run: |
        twine upload dist/*
